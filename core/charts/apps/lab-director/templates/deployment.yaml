apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lab-director.fullname" . }}
  labels:
    {{- include "lab-director.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "lab-director.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "lab-director.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: lab-director
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: {{ .Values.port }}
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.secretName }}
                  key: {{ .Values.database.secretKey }}
            - name: OPERATOR_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: OPERATOR_URL
            {{- if .Values.env.TEMPORAL_HOST_PORT }}
            - name: TEMPORAL_HOST_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: TEMPORAL_HOST_PORT
            {{- end }}
            {{- if .Values.env.TEMPORAL_NAMESPACE }}
            - name: TEMPORAL_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: TEMPORAL_NAMESPACE
            {{- end }}
            {{- if .Values.env.LITELLM_URL }}
            - name: LITELLM_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: LITELLM_URL
            {{- end }}
            {{- if .Values.litellmSecret.enabled }}
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.litellmSecret.secretName }}
                  key: {{ .Values.litellmSecret.secretKey }}
            {{- end }}
            {{- if .Values.env.PLANE_API_URL }}
            - name: PLANE_API_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: PLANE_API_URL
            {{- end }}
            {{- if .Values.env.PLANE_WORKSPACE_SLUG }}
            - name: PLANE_WORKSPACE_SLUG
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: PLANE_WORKSPACE_SLUG
            {{- end }}
            {{- if .Values.env.PLANE_PROJECT_ID }}
            - name: PLANE_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: PLANE_PROJECT_ID
            {{- end }}
            {{- if .Values.env.OUTLINE_API_URL }}
            - name: OUTLINE_API_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: OUTLINE_API_URL
            {{- end }}
            {{- if .Values.env.FORGEJO_API_URL }}
            - name: FORGEJO_API_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "lab-director.fullname" . }}
                  key: FORGEJO_API_URL
            {{- end }}
            {{- if .Values.serviceCredentials.enabled }}
            - name: PLANE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.serviceCredentials.secretName }}
                  key: PLANE_API_KEY
            - name: OUTLINE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.serviceCredentials.secretName }}
                  key: OUTLINE_API_KEY
            - name: FORGEJO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.serviceCredentials.secretName }}
                  key: FORGEJO_TOKEN
            {{- end }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
