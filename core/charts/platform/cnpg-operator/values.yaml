# cnpg-operator (cloudnative-pg) — Helm values
# Source chart: https://cloudnative-pg.github.io/charts / cloudnative-pg
crds:
  create: true
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
