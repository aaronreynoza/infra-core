# External Secrets Operator — syncs secrets from source stores into K8s
# Chart: external-secrets/external-secrets

# Use Harbor GHCR pull-through cache
global:
  repository: harbor.aaron.reynoza.org/ghcr-cache

image:
  repository: harbor.aaron.reynoza.org/ghcr-cache/external-secrets/external-secrets
  pullPolicy: IfNotPresent

# Controller
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

# Webhook
webhook:
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi

# Cert controller
certController:
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi

# CRDs managed by Helm
installCRDs: true

# Metrics for Prometheus
serviceMonitor:
  enabled: true
  namespace: monitoring
