---
# Default management network CIDR — override in inventory or group_vars
mgmt_network_cidr: "CHANGE_ME_MGMT_CIDR"
# SSH AllowUsers pattern — must match your management network
mgmt_ssh_allow_pattern: "CHANGE_ME_MGMT_PATTERN"  # e.g., "192.168.1.*"
