apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: temporal
  namespace: temporal
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: cilium-system
  hostnames:
    - "temporal.aaron.reynoza.org"
  rules:
    - backendRefs:
        - name: temporal-web
          port: 8080
