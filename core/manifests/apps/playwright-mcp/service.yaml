apiVersion: v1
kind: Service
metadata:
  name: playwright-mcp
  namespace: agent-tools
spec:
  selector:
    app: playwright-mcp
  ports:
    - port: 8931
      targetPort: 8931
      nodePort: 30931
      protocol: TCP
  type: NodePort
